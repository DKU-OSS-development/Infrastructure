# ------------------------------------------------------
# 1. [보안 필수] 시크릿 및 인증 키 (절대 커밋 금지)
# ------------------------------------------------------
# 특정 폴더의 시크릿 파일
k8s/00-common/secret.yaml
k8s/00-common/secrets.yaml

# 혹시 다른 폴더에 생길지 모르는 시크릿 파일들 (재귀적 무시)
**/secret.yaml
**/secrets.yaml

# 환경변수 파일
.env
.env.*

# 인증서 및 키 파일
*.pem
*.key
*.crt
*.cer
id_rsa
id_rsa.pub
kubeconfig

# ------------------------------------------------------
# 2. [OS 및 에디터] 시스템 자동 생성 파일
# ------------------------------------------------------
# Windows (작업 PC에서 생길 수 있음)
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# Mac (팀원이 맥북을 쓸 경우 대비)
.DS_Store
.AppleDouble
.LSOverride

# VS Code (개인 설정 폴더)
.vscode/
!.vscode/extensions.json  # (예외: 추천 확장프로그램 목록은 공유해도 됨)

# ------------------------------------------------------
# 3. [기타] 로그 및 임시 파일
# ------------------------------------------------------
*.log
tmp/
temp/
backup/