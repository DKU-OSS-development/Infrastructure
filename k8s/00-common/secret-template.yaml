apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: claude-app
type: Opaque
stringData:
  # 여기에 실제 값을 넣고 저장하세요 (이 파일은 커밋해도 됨. 실제 키는 secret.yaml에!)
  CLAUDE_API_KEY: "YOUR_API_KEY_HERE"
  DB_PASSWORD: "YOUR_DB_PASSWORD_HERE"