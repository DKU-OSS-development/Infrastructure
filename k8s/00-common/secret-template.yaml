apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: claude-app
type: Opaque
stringData:
  # 여기에 실제 값을 넣고 저장할 것 -> 깃헙에 공개 되면 안됨(실제 키는 secret.yaml에!)
  # 현재 테스트 키 값으로 설정 -> 배포 후 실제 키로 변경 필요
  CLAUDE_API_KEY: TEST_KEY #"YOUR_API_KEY_HERE" 
  DB_PASSWORD: "YOUR_DB_PASSWORD_HERE"